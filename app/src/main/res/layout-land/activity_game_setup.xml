<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    tools:context=".GameSetupActivity">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >

            <TextView
                android:id="@+id/textViewGameSetup"
                style="@style/heading"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="24dp"
                android:text="@string/gameSetup"
                android:textColor="?attr/colorPrimary" />

            <Button
                android:id="@+id/buttonGameVsAndroid"
                style="@style/buttonPrimary"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="32dp"
                android:backgroundTint="?attr/colorPrimary"
                android:text="@string/gameVsAndroid"
                android:textColor="?attr/android:background" />

            <Button
                android:id="@+id/buttonGameVsPlayer"
                style="@style/buttonPrimary"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="18dp"
                android:backgroundTint="?attr/colorPrimary"
                android:text="@string/gameVsPlayer"
                android:textColor="?attr/android:background" />

            <Button
                android:id="@+id/buttonGameSetupHome"
                style="@style/buttonSecondary"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="18dp"
                android:layout_marginBottom="24dp"
                android:backgroundTint="?attr/colorSecondary"
                android:text="@string/buttonHome"
                android:textColor="?attr/android:background" />
        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>